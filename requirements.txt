# Telegram bot framework (async)
python-telegram-bot[ext]>=21.0,<22.0

# Google Sheets access via service account
gspread>=6.0,<7.0
google-auth>=2.0,<3.0

# HTTP client for the exchange-rate API
httpx>=0.27,<1.0

# Async PostgreSQL driver
asyncpg>=0.29,<0.31

# Fuzzy place-name matching for smart categorization
rapidfuzz>=3.0,<4.0

# Load config / secrets from environment
python-dotenv>=1.0,<2.0
